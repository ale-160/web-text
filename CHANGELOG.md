# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.1] - 2026-08-03

### 新增

- **多文档管理**：左侧边栏管理多个文档，支持搜索、重命名、删除，历史版本按文档隔离
- **IndexedDB 存储**：内容与历史迁移到 IndexedDB（容量更大更可靠），localStorage 旧数据自动迁移
- **一键备份/恢复**：全部数据导出为 JSON 文件，可随时恢复
- **导出 HTML**：一键导出为独立 HTML 文档（含代码高亮样式）
- **拖拽导入**：拖入 `.md` 文件自动新建文档
- **打开本地文件**：直接编辑本地 `.md` 文件，编辑自动写回磁盘（Chrome/Edge，File System Access API）
- **专注模式增强**：专注模式下可切换编辑/分屏/预览视图、全屏，并显示字数统计
- **键盘快捷键**：`Ctrl/⌘+1/2/3` 切换视图，`Ctrl/⌘+Shift+F` 全屏，`Ctrl/⌘+S` 立即保存
- **侧边栏折叠**：文档列表可收起/展开，状态持久化
- **字数统计**：实时显示字数（中文按字、英文按词）与字符数
- **帮助面板**：新增键盘快捷键说明

### 安全

- 预览渲染增加 `rehype-sanitize`，防止 XSS（配合 `rehype-raw` 使用）

### 修复

- 切换语言时可能覆盖未保存内容的隐患
- 修复历史版本存储结构，固定版本与普通版本统一管理
